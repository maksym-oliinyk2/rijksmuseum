<resources>

    <!-- Base application theme. -->
    <style name="RijksmuseumTheme">
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="RijksmuseumThemeSplash" parent="Theme.SplashScreen">
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="windowSplashScreenBackground">@color/colorBackground</item>
        <!--<item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>-->
        <item name="postSplashScreenTheme">@style/RijksmuseumTheme</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>
